import { TestBed, inject } from '@angular/core/testing';
import { TeamsFormPresenter } from './teams-form.presenter';


describe('TeamsFormPresenter', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [TeamsFormPresenter]
    });
  });

  // it('should be created', inject([TeamsFormPresenter], (service: TeamsFormPresenter) => {
  //   expect(service).toBeTruthy();
  // }));
});
