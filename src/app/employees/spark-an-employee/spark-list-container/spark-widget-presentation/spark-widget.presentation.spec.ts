import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SparkWidgetPresentation } from './spark-widget.presentation';

describe('SparkWidgetComponent', () => {
  let component: SparkWidgetPresentation;
  let fixture: ComponentFixture<SparkWidgetPresentation>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ SparkWidgetPresentation ]
  //   })
  //   .compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(SparkWidgetPresentation);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
