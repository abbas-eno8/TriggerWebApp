import { TestBed, inject } from '@angular/core/testing';
import { SparkListPresenter } from './spark-list-presenter';

describe('SparkListPresenter', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [SparkListPresenter]
    });
  });

  // it('should be created', inject([SparkListPresenter], (service: SparkListPresenter) => {
  //   expect(service).toBeTruthy();
  // }));
});
