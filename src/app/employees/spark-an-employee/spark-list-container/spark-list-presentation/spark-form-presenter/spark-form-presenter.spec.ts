import { TestBed, inject } from '@angular/core/testing';
import { SparkFormPresenter } from './spark-form-presenter';


describe('SparkFormPresenter', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [SparkFormPresenter]
    });
  });

  // it('should be created', inject([SparkFormPresenter], (service: SparkFormPresenter) => {
  //   expect(service).toBeTruthy();
  // }));
});
