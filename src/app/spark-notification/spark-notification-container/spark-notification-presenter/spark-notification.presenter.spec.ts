import { TestBed, inject } from '@angular/core/testing';

import { SparkNotificationPresenter } from './spark-notification.presenter';

describe('SparkNotificationPresenter', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [SparkNotificationPresenter]
    });
  });

  // it('should be created', inject([SparkNotificationPresenter], (service: SparkNotificationPresenter) => {
  //   expect(service).toBeTruthy();
  // }));
});
