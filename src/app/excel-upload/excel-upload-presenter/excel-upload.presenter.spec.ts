import { TestBed, inject } from '@angular/core/testing';
import { ExcelUploadPresenter } from './excel-upload.presenter';

describe('ExcelUploadPresenter', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ExcelUploadPresenter]
    });
  });

  // it('should be created', inject([ExcelUploadPresenter], (service: ExcelUploadPresenter) => {
  //   expect(service).toBeTruthy();
  // }));
});
