import { SparkSearchPipe } from './spark-search.pipe';

describe('SparkSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new SparkSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
