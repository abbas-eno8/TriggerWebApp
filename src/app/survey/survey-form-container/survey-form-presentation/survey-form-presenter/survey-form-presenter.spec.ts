import { TestBed } from '@angular/core/testing';

import { SurveyFormPresenter } from './survey-form-presenter';

describe('SurveyFormPresenter', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  // it('should be created', () => {
  //   const service: SurveyFormPresenter = TestBed.get(SurveyFormPresenter);
  //   expect(service).toBeTruthy();
  // });
});
