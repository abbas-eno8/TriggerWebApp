import { TestBed } from '@angular/core/testing';

import { SurveyDetailsPresenter } from './survey-details.presenter';

describe('SurveyDetailsPresenterService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  // it('should be created', () => {
  //   const service: SurveyDetailsPresenter = TestBed.get(SurveyDetailsPresenter);
  //   expect(service).toBeTruthy();
  // });
});
