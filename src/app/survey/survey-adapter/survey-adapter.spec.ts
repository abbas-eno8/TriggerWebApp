import { TestBed } from '@angular/core/testing';
import { SurveyListAdapter } from './survey-adapter';


describe('SurveyListAdapter', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

//   it('should be created', () => {
//     const service: SurveyListAdapter = TestBed.get(SurveyListAdapter);
//     expect(service).toBeTruthy();
//   });
});
