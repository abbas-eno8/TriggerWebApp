import { TestBed } from '@angular/core/testing';
import { SurveyPreviewPresenter } from './survey-preview-presenter';


describe('SurveyPreviewPresenter', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  // it('should be created', () => {
  //   const service: SurveyPreviewPresenter = TestBed.get(SurveyPreviewPresenter);
  //   expect(service).toBeTruthy();
  // });
});
