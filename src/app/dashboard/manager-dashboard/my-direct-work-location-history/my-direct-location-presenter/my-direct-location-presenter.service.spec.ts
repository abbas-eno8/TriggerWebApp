import { TestBed } from '@angular/core/testing';

import { MyDirectLocationPresenter } from './my-direct-location-presenter.service';

describe('MyDirectLocationPresenter', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  // it('should be created', () => {
  //   const service: MyDirectLocationPresenterService = TestBed.get(MyDirectLocationPresenterService);
  //   expect(service).toBeTruthy();
  // });
});
