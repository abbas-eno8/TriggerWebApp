import { TestBed } from '@angular/core/testing';

import { MyDirectWorkLocationHistoryService } from './my-direct-work-location-history.service';

describe('MyDirectWorkLocationHistoryService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  // it('should be created', () => {
  //   const service: MyDirectWorkLocationHistoryService = TestBed.get(MyDirectWorkLocationHistoryService);
  //   expect(service).toBeTruthy();
  // });
});
