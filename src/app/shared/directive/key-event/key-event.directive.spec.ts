import { KeyEventDirective } from './key-event.directive';

describe('KeyEventDirective', () => {
  it('should create an instance', () => {
    // const directive = new KeyEventDirective();
    // expect(directive).toBeTruthy();
  });
});
