import { FocusInitDirective } from './focus-init.directive';

describe('FocusInitDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new FocusInitDirective();
  //   expect(directive).toBeTruthy();
  // });
});
