// import { DateTimeStampComponent } from './date-time-stamp.directive';

describe('DateTimeStampDirective', () => {
  it('should create an instance', () => {
    //const directive = new DateTimeStamp();
    //expect(directive).toBeTruthy();
  });
});
